// window.addEventListener("scroll", function () {
//   const scrollPosition = window.scrollY;
//   const item = document.querySelector(".animated-item");
//   const moveUp = Math.min(scrollPosition * 0.5, 100); // Limita a animação a 100px
//   item.style.transform = `translateY(${100 - moveUp}px)`;
// });
